Поместите сюда изображения для нативного splash:

- loadPage.png  — фоновое изображение для splash
- loadLogo.png  — логотип для Android 12 (и выше)

Имена файлов должны совпадать с путями в pubspec.yaml: `assets/app/loadPage.png` и `assets/app/loadLogo.png`.
